<%= simple_form_for @card do |c| %>
  <%= c.input :original_text, label: "front of card" %>
  <%= c.input :translated_text, label: "back of card" %>
  <%= c.input :user_id, as: "hidden", input_html: {value: current_user.id} %>
  <%= c.button :submit %>
<% end %>
